# StreamSocial system requirements (Day 5 snapshot)
# This is a Java / Maven / Docker project — not Python.
# Install these on the host before running ./start.sh

java>=17
maven>=3.8
docker>=24
docker-compose>=2.20

# Recommended
# - 4GB+ RAM available to Docker
# - Internet access to Maven Central (first build)
