<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.streamsocial</groupId>
  <artifactId>streamsocial</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>StreamSocial</name>
  <description>
    Kafka Mastery: Building StreamSocial - Java &amp; Spring Boot Edition.
    Reactor project. Modules are added, never removed, one per lesson.
  </description>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <junit.version>5.10.2</junit.version>
    <assertj.version>3.25.3</assertj.version>
    <hibernate.validator.version>8.0.1.Final</hibernate.validator.version>
    <jakarta.validation.version>3.0.2</jakarta.validation.version>
    <jakarta.el.version>4.0.2</jakarta.el.version>
    <kafka.clients.version>3.7.1</kafka.clients.version>
    <testcontainers.version>1.19.8</testcontainers.version>
    <spring.boot.version>3.3.2</spring.boot.version>
    <jmh.version>1.37</jmh.version>
  </properties>

  <!--
    Module list. Day 1 introduces streamsocial-common only.
    Later lessons append modules here; this list is never pruned.
  -->
  <modules>
    <module>streamsocial-common</module>
    <module>streamsocial-producer-service</module>
    <module>streamsocial-engagement-consumer</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!--
        Day 4: first Spring Boot module. Imported here, once, at the
        reactor root so every future Spring-based module (Day 5's
        consumer, Day 48's gateway, Day 57's microservices) shares one
        managed version instead of each module pinning its own.
      -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring.boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.2.5</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
